### To run the application, execute the following command:

1. Ensure `start.sh` has execute permissions:

    ```sh
    chmod +x ./start.sh
    ```

2. Run the script:

    ```sh
    ./start.sh
    ```
